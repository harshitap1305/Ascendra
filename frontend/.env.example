# Frontend Environment Variables for Production Deployment
# During local development (npm run dev), Vite automatically proxies requests from '/api' to 'http://localhost:8000'.
# When deploying to Vercel, Netlify, Render, or Cloudflare Pages, add the variable below to your dashboard settings:

VITE_API_URL=https://your-backend-service.onrender.com/api
